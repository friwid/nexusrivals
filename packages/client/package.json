{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "dev": "vite  --host",
    "build-test": "NODE_ENV=test vite build --mode production",
    "build-live": "NODE_ENV=production vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.15",
    "server": "workspace:*"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "vite": "^5.1.0"
  }
}

